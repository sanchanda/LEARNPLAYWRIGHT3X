// console.log(MAX_RETRIES); // TDZ
// const MAX_RETRIES = 3;
// MAX_RETRIES = 4; //Throws TypeError: Assignment to constant variable is not allowed multiple times

const score = 100
// score = score + 10; //throws TypeError: Assignment to constant variable is not allowed multiple times

const pi = 3.14;
const prod_api_url = "https://app.vwo.com/#login";
//prod_api_url = "https://google.com" //throws TypeError: Assignment to constant variable is not allowed multiple times
const qa_api_url = "https://qa.vwo.com/#login";

let abc = "anil";
console.log(abc);